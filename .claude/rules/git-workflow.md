## Git Workflow

- `main`: stable/production. Tags for releases.
- `feat/<name>`: feature branches from main, one MR per feature.
- `fix/<name>`: fix branches from main.
- `refactor/<name>`: refactoring branches from main.

Do not commit directly to main unless explicitly asked.
